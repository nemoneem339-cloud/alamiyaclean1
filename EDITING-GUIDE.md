# 📝 دليل التعديل - العالمية كلين Astro

هذا الدليل يشرح كيفية تعديل محتوى الموقع بسهولة لغير المبرمجين.

---

## 📁 هيكل المشروع

```
src/
├── data/
│   └── siteInfo.ts     ← ملف البيانات الرئيسي (الأهم!)
├── pages/
│   ├── index.astro     ← الصفحة الرئيسية
│   ├── about.astro     ← عن الشركة
│   ├── contact.astro   ← اتصل بنا
│   └── services/
│       ├── index.astro         ← جميع الخدمات
│       ├── cleaning-villas.astro
│       ├── pest-control.astro
│       └── water-tanks.astro
├── components/         ← مكونات مشتركة (Header, Footer)
└── layouts/           ← القالب الأساسي
```

---

## 🔧 تعديل البيانات الأساسية

### 1. معلومات الشركة (الهاتف، العنوان، إلخ)

افتح الملف: `src/data/siteInfo.ts`

```typescript
export const company = {
  name: "العالمية كلين",           // ← اسم الشركة
  phone: "0500801225",            // ← رقم الهاتف
  whatsapp: "966500801225",       // ← رقم الواتساب (بدون +)
  email: "info@alamiyaclean.sa",  // ← البريد الإلكتروني
  address: "الرياض، حي الملقا",   // ← العنوان
  // ...
};
```

### 2. الخدمات

في نفس الملف `src/data/siteInfo.ts`:

```typescript
export const services = [
  {
    id: "cleaning-villas",
    slug: "cleaning-villas",      // ← رابط الصفحة
    title: "تنظيف فلل وقصور VIP", // ← العنوان
    shortDesc: "خدمة النخبة...",  // ← الوصف القصير
    icon: "🏠",                   // ← الأيقونة (إيموجي)
    features: [...],              // ← المميزات
    benefits: [...],              // ← الفوائد
    image: "https://...",         // ← صورة الخدمة
    faqs: [...],                  // ← الأسئلة الشائعة
  },
  // ... المزيد من الخدمات
];
```

### 3. آراء العملاء

```typescript
export const testimonials = [
  {
    name: "أبو عبدالعزيز",
    role: "عميل VIP",
    content: "بصراحة شغل يبيض الوجه...",
    rating: 5,  // ← من 1 إلى 5
  },
  // ...
];
```

### 4. الأسئلة الشائعة

```typescript
export const faqs = [
  {
    question: "هل العمالة نظامية وموثوقة؟",
    answer: "نعم، جميع طاقم العمل لدينا...",
  },
  // ...
];
```

---

## 🖼️ تغيير الصور

### صور الخدمات

في `src/data/siteInfo.ts`، عدّل رابط الصورة:

```typescript
{
  title: "تنظيف فلل",
  image: "https://images.unsplash.com/photo-...", // ← رابط الصورة الجديد
}
```

### صورة Hero (الخلفية الرئيسية)

افتح `src/pages/index.astro` وابحث عن:

```html
<img 
  src="https://images.unsplash.com/photo-1600585154340-be6161a56a0c..."
  alt="منزل نظيف فاخر"
  ...
/>
```

غيّر رابط `src` للصورة الجديدة.

---

## 📄 تعديل صفحة معينة

### الصفحة الرئيسية

افتح: `src/pages/index.astro`

للتعديل على العنوان الرئيسي:

```html
<h1 class="...">
  نحن نعتني بمنزلك، <br/> 
  <span class="...">
    لتستمتع بحياتك   <!-- ← غيّر هذا النص -->
  </span>
</h1>
```

### صفحة عن الشركة

افتح: `src/pages/about.astro`

ابحث عن النصوص وعدّلها مباشرة.

### صفحة خدمة معينة

مثلاً لتعديل صفحة تنظيف الفلل:

افتح: `src/pages/services/cleaning-villas.astro`

---

## ➕ إضافة خدمة جديدة

### خطوة 1: إضافة البيانات

في `src/data/siteInfo.ts`، أضف خدمة جديدة لمصفوفة `services`:

```typescript
export const services = [
  // الخدمات الحالية...
  
  // الخدمة الجديدة:
  {
    id: "new-service",
    slug: "new-service",
    title: "اسم الخدمة الجديدة",
    shortDesc: "وصف قصير للخدمة",
    fullDesc: "وصف تفصيلي...",
    icon: "🧹",
    features: ["ميزة 1", "ميزة 2"],
    benefits: ["فائدة 1", "فائدة 2"],
    priceRange: "$$",
    image: "https://...",
    faqs: [
      { question: "سؤال؟", answer: "الجواب" }
    ],
  },
];
```

### خطوة 2: إنشاء صفحة للخدمة

انسخ ملف `src/pages/services/cleaning-villas.astro`

وأعد تسميته إلى `src/pages/services/new-service.astro`

ثم عدّل المحتوى.

---

## 🎨 تغيير الألوان

افتح: `tailwind.config.mjs`

```javascript
colors: {
  primary: '#1e3a5f',    // ← اللون الأساسي (أزرق داكن)
  secondary: '#06b6d4',  // ← اللون الثانوي (سماوي)
}
```

---

## 🔤 تغيير الخط

افتح: `tailwind.config.mjs`

```javascript
fontFamily: {
  sans: ['Cairo', 'Tajawal', 'sans-serif'],
}
```

لتغيير الخط، استبدل Cairo بخط آخر مثل:
- `Tajawal`
- `IBM Plex Sans Arabic`
- `Noto Sans Arabic`

---

## 📧 تفعيل نموذج الاتصال

النموذج في `src/pages/contact.astro` يحتاج لخدمة خارجية.

### الخيار 1: Formspree (الأسهل)

1. اذهب إلى [formspree.io](https://formspree.io)
2. أنشئ حساب مجاني
3. أنشئ نموذج جديد
4. انسخ الـ form ID
5. في `contact.astro`، غيّر:

```html
<form action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
```

استبدل `YOUR_FORM_ID` بالـ ID الخاص بك.

### الخيار 2: Netlify Forms

إذا رفعت الموقع على Netlify، أضف:

```html
<form name="contact" method="POST" data-netlify="true">
```

---

## 🚀 رفع الموقع

### على Netlify (مجاني)

1. ارفع المشروع على GitHub
2. اذهب إلى [netlify.com](https://netlify.com)
3. اختر "Add new site" > "Import an existing project"
4. اختر repository من GitHub
5. الإعدادات:
   - Build command: `npm run build`
   - Publish directory: `dist`
6. اضغط Deploy

### على Hostinger

1. شغّل الأمر: `npm run build`
2. سينتج مجلد `dist/`
3. ارفع محتويات `dist/` إلى `public_html` في Hostinger

---

## 🛠️ الأوامر الأساسية

```bash
# تشغيل الموقع محلياً للمعاينة
npm run dev

# بناء الموقع للإنتاج
npm run build

# معاينة النسخة النهائية
npm run preview
```

---

## ❓ مشاكل شائعة

### الموقع لا يعمل بعد التعديل

تأكد أنك لم تحذف فاصلة أو قوس بالخطأ في ملف TypeScript.

### الصورة لا تظهر

تأكد أن رابط الصورة صحيح ويبدأ بـ `https://`

### النص لا يظهر بشكل صحيح

تأكد أن الملف محفوظ بترميز UTF-8.

---

## 📞 للمساعدة

إذا واجهت أي مشكلة، تواصل مع المطور.
